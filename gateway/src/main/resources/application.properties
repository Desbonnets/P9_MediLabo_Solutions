spring.application.name=gateway

server:
    port: 8080

spring:
    cloud:
        gateway:
            routes:
                - id: user-service
                    uri: http://back:8081
                    predicates:
                        - Path=/api/users/**
